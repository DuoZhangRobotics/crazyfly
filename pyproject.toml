[project]
name = "crazyflie-first-flight"
version = "0.1.0"
description = "Safety-first connection and hover checks for a Crazyflie 2.x"
requires-python = ">=3.10,<3.14"
dependencies = [
    "cflib==0.1.33",
]

[tool.uv]
package = false
